# =============================================================================
# Environment Variables for Audio Transcript Analysis App
# Copy this file to .env and fill in your values
# =============================================================================

# -----------------------------------------------------------------------------
# Firebase Authentication Configuration (required)
# -----------------------------------------------------------------------------

# Get these values from Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Select your project (or create a new one)
# 3. Go to Project Settings (gear icon) > General
# 4. Scroll down to "Your apps" and select/create a Web app
# 5. Copy the config values below

VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890

# Enable Google Sign-In:
# 1. Go to Firebase Console > Authentication > Sign-in method
# 2. Enable "Google" provider
# 3. Add your domain to authorized domains (localhost is included by default)

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

# Gemini API Key (required for client-side processing)
# Get yours at: https://makersuite.google.com/app/apikey
# Note: When using Cloud Functions, set this in Firebase secrets instead
GEMINI_API_KEY=your_gemini_api_key_here

# Also set with VITE_ prefix for Vite build
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# -----------------------------------------------------------------------------
# Backend Mode Selection
# -----------------------------------------------------------------------------

# Set to 'true' to use Firestore (cloud storage with sync)
# Set to 'false' or omit to use IndexedDB (local-only mode)
VITE_USE_FIRESTORE=false

# Set to 'true' to connect to Firebase Emulators during development
VITE_USE_FIREBASE_EMULATORS=false

# -----------------------------------------------------------------------------
# Deployment Configuration (for deploy.sh)
# -----------------------------------------------------------------------------

# Google Cloud Project ID (required)
GCP_PROJECT_ID=your-gcp-project-id

# Cloud Run region (optional, defaults to us-west1)
GCP_REGION=us-west1

# Cloud Run service name (optional, defaults to audio-transcript-app)
GCP_SERVICE_NAME=audio-transcript-app

# -----------------------------------------------------------------------------
# Alignment Service Configuration (Phase 2 - WhisperX)
# -----------------------------------------------------------------------------

# Replicate API Token (required for timestamp alignment)
# Get yours at: https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=your_replicate_api_token_here

# Alignment Service URL (set after deploying alignment-service to Cloud Run)
# Leave empty to use localhost:8080 for local development
ALIGNMENT_SERVICE_URL=
